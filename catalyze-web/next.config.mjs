/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'xmwoplnoukaslunlmpqe.supabase.co',
      },
    ],
  },
}

export default nextConfig
