Flask>=2.0
requests
python-dotenv
flask-cors
RPi.GPIO
ultralytics
opencv-python
# torch: install separately from the CPU index:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
# picamera2: provided by apt (python3-picamera2), not pip
# venv must be created with: python3 -m venv --system-site-packages .venv